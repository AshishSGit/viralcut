[phases.setup]
aptPkgs = ["ffmpeg", "python3", "python3-pip"]

[phases.install]
cmds = ["npm ci", "pip install yt-dlp --break-system-packages"]

[phases.build]
cmds = ["npm run build", "which yt-dlp || pip install yt-dlp --break-system-packages"]
